/**
 * 花里胡哨
 *
 * 只为转换得更好
 */
const $ = require('gogocode')
const t = require("@babel/types")
const clone = require("clone")

var appRoot = "../../.."
const ggcUtils = require(appRoot + "/src/utils/ggcUtils")


/**
 * 花里胡哨
 * @param {*} $ast
 * @param {*} fileKey
 */
function transformToBetter ($jsAst, $wxml) {


}

module.exports = { transformToBetter }
